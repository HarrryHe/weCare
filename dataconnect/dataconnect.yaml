specVersion: "v1beta"
serviceId: "weCare"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "weCare-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
